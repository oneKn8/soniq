# Test-only dependencies for the soniq-agent unit test suite.
# These are NOT required to run the agent in production (see requirements.txt).
# The suite mocks the livekit SDK via sys.modules, so livekit-agents does not
# need to be installed to run the tests. httpx is pinned to match the runtime
# dependency so the response/exception types under test are the real ones.
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27.0
